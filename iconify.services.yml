services:
  iconify.manager:
    class: Drupal\iconify\IconifyManager
    arguments: ["@entity.manager", "@cache.discovery"]

  plugin.manager.iconify.info:
    class: Drupal\iconify\IconifyInfoManager
    arguments: ['@module_handler', '@cache.discovery']

